# 复赛推理镜像依赖（基础镜像 pytorch/pytorch:2.5.1-cuda12.4-cudnn9-runtime
# 已自带 torch 2.5.1+cu124，这里【不要】再装 torch，以免覆盖 CUDA 版本）。
# transformers 与 pynvml 由 Dockerfile 单独安装（版本通过 build-arg 注入）。
numpy
scikit-learn
pyyaml
tqdm
torchaudio==2.5.1
